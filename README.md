# cdripper
Automation of extracting the Audio Files from a CD, ejecting the CD-Trace and waiting for another CD


## Requirenments
```
python, pip, swig, libcdio
```
After installing those packages:

```bash
pip install -r requirement.txt
```